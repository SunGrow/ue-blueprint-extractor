# ue-blueprint-extractor

- Git workflow: by default, commit and push directly to `master`.
- Do not create or use topic branches unless the user explicitly asks for one.
- In a dirty worktree, stage and commit only the files relevant to the current task.
